---
description: Remove safe local Wily temporary artifacts
argument-hint: '[--yes]'
disable-model-invocation: true
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, AskUserQuestion
---

Run the `wily-clean` skill to inspect or remove safe local temporary artifacts.

Default mode is dry-run. Use `--yes` only when the user explicitly wants to delete the listed local cleanup candidates.

Arguments:

$ARGUMENTS
