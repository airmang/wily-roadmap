# Handoff

Start here if continuing the current local edits.

Before editing, inspect:

```bash
git status --short
git diff -- skills tests
```

Then decide whether the existing response-style patch is ready for review or needs a small cleanup.
