# Verification

Run:

```bash
python3 -m unittest tests.test_wily_command_skills
python3 -m unittest discover
```

Manually inspect representative skill files for concise, non-duplicative response guidance.
