# Notes

- This phase is intentionally after self-update so the resulting guidance and implementation can be distributed through the new update path.
- A compact snapshot mode may be better than a continuous pane inside Codex app.
