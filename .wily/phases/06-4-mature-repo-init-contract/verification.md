# Verification

Run:

```bash
python3 -m unittest tests.test_wily_cli
python3 -m unittest discover
python3 -m py_compile scripts/wily.py
```

Also manually inspect `$wily-init` output in a temporary mature-looking repository fixture.
