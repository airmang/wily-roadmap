# Handoff

Start by capturing current `$wily-init` behavior for an empty repo, a mature repo with files, and a partial `.wily` directory.

This phase can run in parallel with phases 06-1, 06-2, 06-3, and 06-5.
