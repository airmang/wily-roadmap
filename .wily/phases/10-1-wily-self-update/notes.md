# Notes

- The new bootstrap zip should include this command so existing recipients only need one more manual zip delivery.
- `.codex-plugin/plugin.json` remains the canonical version display.
- Keep `.claude-plugin/plugin.json` version aligned when present.
