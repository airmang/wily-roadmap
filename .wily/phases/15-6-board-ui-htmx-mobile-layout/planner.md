# Planner

Recommended planner: manual UI implementation, with Claude ownership for visual design.

Use real SQLite fixtures from sync ingestion once available; until then use explicit test fixtures matching the schema.

