# Execution Prompt

Execute Stage s15: Wily Board external dashboard implementation.

Use `docs/wily-board-plan.md` as the source handoff. Keep `wily-roadmap` as the plugin and sync target; build the board as a separate lightweight web application unless the user explicitly changes that boundary.

